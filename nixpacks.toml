# Railway Nixpacks build configuration
# Ensures devDependencies are available for the build step (dotenv-flow, npm-force-resolutions)

[phases.install]
cmds = ['npm install --include=dev']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node src/server.js'
